<Page
    x:Class="XmaX.Pages.SettingsContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <ScrollViewer Padding="24,16" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="4" MaxWidth="600">

            <!-- Language -->
            <controls:SettingsCard x:Name="LanguageCard" Header="Language" Margin="0">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xEBBE;" FontFamily="/Assets/tabler-icons-300.ttf#tabler-icons" FontSize="24" />
                </controls:SettingsCard.HeaderIcon>
                <ComboBox x:Name="LanguageCombo" SelectionChanged="OnLanguageChanged" />
            </controls:SettingsCard>

            <!-- Theme -->
            <controls:SettingsCard x:Name="ThemeCard" Header="Theme" Margin="0">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xEBB8;" FontFamily="/Assets/tabler-icons-300.ttf#tabler-icons" FontSize="24" />
                </controls:SettingsCard.HeaderIcon>
                <ComboBox x:Name="ThemeCombo" SelectionChanged="OnThemeChanged" />
            </controls:SettingsCard>

            <!-- Persist -->
            <controls:SettingsCard x:Name="PersistCard" Description="Description" Margin="0">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xEA5E;" FontFamily="/Assets/tabler-icons-300.ttf#tabler-icons" FontSize="24" />
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="PersistToggle" Toggled="OnPersistToggled" />
            </controls:SettingsCard>

            <!-- Auto-start -->
            <controls:SettingsCard x:Name="AutoStartCard" Description="Description" Margin="0">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xEB0D;" FontFamily="/Assets/tabler-icons-300.ttf#tabler-icons" FontSize="24" />
                </controls:SettingsCard.HeaderIcon>
                <ToggleSwitch x:Name="AutoStartToggle" Toggled="OnAutoStartToggled" />
            </controls:SettingsCard>

            <!-- Variable Graphics Memory (UMA) -->
            <controls:SettingsCard x:Name="UmaCard" Description="Description" Visibility="Collapsed">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xEA5D;" FontFamily="/Assets/tabler-icons-300.ttf#tabler-icons" FontSize="24" />
                </controls:SettingsCard.HeaderIcon>
                <ComboBox x:Name="UmaCombo" SelectionChanged="OnUmaSelectionChanged" />
            </controls:SettingsCard>

            <!-- Sub-page navigation -->
            <StackPanel Spacing="4" Margin="0,0,0,0">
                <controls:SettingsCard x:Name="ProfilesCard" Header="Profiles" Description="Manage performance profiles" Margin="0" IsClickEnabled="True" Click="OnNavigateProfiles">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xEC38;" FontFamily="/Assets/tabler-icons-300.ttf#tabler-icons" FontSize="24" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>

                <controls:SettingsCard x:Name="CoolingCard" Header="Fan Curves" Description="Customize cooling profiles" Margin="0" IsClickEnabled="True" Click="OnNavigateCooling">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xFDB3;" FontFamily="/Assets/tabler-icons-300.ttf#tabler-icons" FontSize="24" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>

                <controls:SettingsCard x:Name="PowerStatesCard" Header="Power Limits" Description="Configure power state assignments" Margin="0" IsClickEnabled="True" Click="OnNavigatePowerStates">
                    <controls:SettingsCard.HeaderIcon>
                        <FontIcon Glyph="&#xEA38;" FontFamily="/Assets/tabler-icons-300.ttf#tabler-icons" FontSize="24" />
                    </controls:SettingsCard.HeaderIcon>
                </controls:SettingsCard>
            </StackPanel>

            <!-- Factory reset (danger zone) -->
            <controls:SettingsCard x:Name="FactoryResetCard" Description="Description" Margin="0,0,0,0">
                <controls:SettingsCard.HeaderIcon>
                    <FontIcon Glyph="&#xFAFD;" FontFamily="/Assets/tabler-icons-300.ttf#tabler-icons" FontSize="24" />
                </controls:SettingsCard.HeaderIcon>
                <Button x:Name="FactoryResetButton"
                        Click="OnFactoryResetClick"
                        Background="{ThemeResource SystemFillColorCriticalBrush}"
                        Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
            </controls:SettingsCard>

        </StackPanel>
    </ScrollViewer>
</Page>
