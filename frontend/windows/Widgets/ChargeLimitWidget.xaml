<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="XmaX.Widgets.ChargeLimitWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Tapped="OnWidgetTapped"
             PointerEntered="OnPointerEntered"
             PointerExited="OnPointerExited">

    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="8"
            Padding="10">
        <Grid RowSpacing="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="CHARGE LIMIT" Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <TextBlock x:Name="ChargeLimitText" Grid.Row="1"
                       Style="{StaticResource SubtitleTextBlockStyle}" />

            <ProgressBar x:Name="ChargeGauge" Grid.Row="3"
                         Minimum="0" Maximum="100" Value="0"
                         Height="8" />

            <TextBlock Grid.Row="4" Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="Tap to cycle" />
        </Grid>
    </Border>
</UserControl>
