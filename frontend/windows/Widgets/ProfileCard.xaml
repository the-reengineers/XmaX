<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="XmaX.Widgets.ProfileCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:widgets="using:XmaX.Widgets"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Border x:Name="RootBorder"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="8" Padding="12" MinWidth="100"
            VerticalAlignment="Stretch"
            PointerEntered="OnPointerEntered" PointerExited="OnPointerExited"
            Tapped="OnTapped">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Spacing="4" Grid.Row="0">
                <TextBlock x:Name="NameText" Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock x:Name="InfoText" Style="{StaticResource CaptionTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <widgets:MiniFanCurveChart x:Name="FanCurveChart" Grid.Row="1" Margin="0,6,0,0" />
        </Grid>
    </Border>

</UserControl>
