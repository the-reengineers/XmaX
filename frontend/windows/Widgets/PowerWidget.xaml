<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="XmaX.Widgets.PowerWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="POWER" Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <TextBlock x:Name="StateText" Grid.Row="1" Style="{StaticResource SubtitleTextBlockStyle}" />
        <Grid x:Name="IconGrid" Grid.Row="2" Width="32" Height="32" VerticalAlignment="Center">
            <TextBlock x:Name="PrimaryIcon"
                       FontFamily="/Assets/materialdesignicons-webfont.ttf#Material Design Icons"
                       Text="&#xF008E;"
                       FontSize="32"
                       Foreground="White"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
            <TextBlock x:Name="SecondaryIcon"
                       FontFamily="/Assets/materialdesignicons-webfont.ttf#Material Design Icons"
                       Text="&#xF140B;"
                       FontSize="12"
                       Foreground="White"
                       HorizontalAlignment="Right" VerticalAlignment="Bottom"
                       Visibility="Collapsed" />
        </Grid>
        <TextBlock x:Name="TdpText" Grid.Row="3" Style="{StaticResource CaptionTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </Grid>

</UserControl>
