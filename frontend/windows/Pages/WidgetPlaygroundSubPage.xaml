<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="XmaX.Pages.WidgetPlaygroundSubPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:wf="using:XmaX.WidgetFramework"
      mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid ColumnSpacing="12" Padding="24,12,24,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="BackBtn" Click="OnBackClick"
                    VerticalAlignment="Center" Padding="8"
                    Background="Transparent" BorderThickness="0">
                <FontIcon Glyph="&#xE72B;" FontSize="14" />
            </Button>
            <TextBlock x:Name="PageTitle" Grid.Column="1"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center" />
        </Grid>

        <wf:WidgetGridHost x:Name="GridHost" Grid.Row="1" />
    </Grid>
</Page>
