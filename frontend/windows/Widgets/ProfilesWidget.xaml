<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="XmaX.Widgets.ProfilesWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid x:Name="RootGrid" Padding="0 4" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   Style="{StaticResource BodyStrongTextBlockStyle}"
                   TextTrimming="CharacterEllipsis"
                   TextWrapping="NoWrap"
                   MaxLines="1" />

        <!-- ScrollViewer with mandatory vertical snap points: each row snaps into place. -->
        <ScrollViewer x:Name="RowsScroller" Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Enabled"
                      VerticalSnapPointsType="Mandatory"
                      VerticalSnapPointsAlignment="Near"
                      HorizontalScrollBarVisibility="Disabled"
                      HorizontalAlignment="Stretch">
            <StackPanel x:Name="RowsPanel" Orientation="Vertical" Spacing="0" />
        </ScrollViewer>
    </Grid>
</UserControl>
