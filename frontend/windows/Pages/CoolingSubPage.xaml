<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="XmaX.Pages.CoolingSubPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <ScrollViewer Padding="24,16" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16" MaxWidth="600">

            <!-- Header -->
            <Grid ColumnSpacing="12" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button x:Name="BackBtn" Click="OnBackClick"
                        VerticalAlignment="Center" Padding="8"
                        Background="Transparent" BorderThickness="0">
                    <FontIcon Glyph="&#xE72B;" FontSize="14" />
                </Button>
                <TextBlock x:Name="FanCurvesTitle" Grid.Column="1"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           VerticalAlignment="Center" />
            </Grid>

            <Button x:Name="CreateFanCurveBtn" Click="OnCreateFanCurveClick"
                    Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Left" />

            <ItemsControl x:Name="FanCurvesList" />

        </StackPanel>
    </ScrollViewer>
</Page>
